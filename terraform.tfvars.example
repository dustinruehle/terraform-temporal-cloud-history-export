# Copy this to terraform.tfvars and fill in your values
# See README.md "Where to Find Configuration Values" for detailed instructions

aws_region           = "us-east-1"
temporal_account_id  = "abc123"                                  # Your account slug (Settings → Account ID)
temporal_external_id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"    # UUID for IAM trust policy
bucket_name          = "your-company-temporal-exports"           # Must be globally unique
namespace_name       = "sandbox"                                 # Your Temporal namespace

# Set API key via environment variable (never commit to version control):
# export TF_VAR_temporal_api_key="your-api-key"
